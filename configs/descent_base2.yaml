task: descent
base: 2
lsb_first: true
bits_train: 48
bits_val: 48
bits_test: 64
batch_size: 512
model:
  type: encoder_classifier
  d_model: 128
  n_heads: 4
  n_layers: 4
  dropout: 0.1
optimizer:
  lr: 0.0005
  weight_decay: 0.01
training:
  max_train_steps: 20000
  eval_every: 500
  checkpoint_every: 1000
  precision: bf16
modal:
  gpu: A10G

