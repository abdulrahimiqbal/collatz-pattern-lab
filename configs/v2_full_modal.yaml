task: v2
base: 2
lsb_first: true
bits_train: 48
bits_val: 48
bits_test: 64
seed: 0
batch_size: 1024
data:
  paths:
    - /mnt/collatz/data/v2/base2_bits48_train_1m.parquet
model:
  type: encoder_classifier
  d_model: 128
  n_heads: 4
  n_layers: 4
  dropout: 0.1
optimizer:
  lr: 0.0005
  weight_decay: 0.01
training:
  max_train_steps: 20000
  eval_every: 500
  checkpoint_every: 1000
  precision: bf16
  grad_clip: 1.0
  disable_progress: true
modal:
  gpu: A10G
notes:
  ood_eval_path: /mnt/collatz/data/v2/base2_bits64_ood_100k.parquet
