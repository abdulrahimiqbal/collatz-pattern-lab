import Collatz.HighParentRootRelative

namespace Collatz

/-
theorem high_parent_d3
  (q : Nat)
  (hq : q > 0)
  (hodd : q % 2 = 1) :
  eventually_descends (2^(32+3) * q - 1) := by
  ...
-/

end Collatz
