import Collatz.HighParentRootRelative

namespace Collatz

/-
theorem goal_d2_symbolic
  (q : Nat)
  (hq : q > 0)
  (hodd : q % 2 = 1) :
  eventually_descends (2^(32+2) * q - 1) := by
  ...
-/

end Collatz
