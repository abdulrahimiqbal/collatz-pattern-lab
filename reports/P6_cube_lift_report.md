# P6 Cube Infinite-Lift Report

- total cubes: `586`
- infinite lift count: `12`
- infinite lift rate: `0.0205`
- status counts: `{'PROVED_INFINITE_ANCESTOR_DESCENT': 12, 'NEEDS_SPLIT': 574}`

## k16

- cubes: `1`
- infinite lift rate: `1.0000`
- statuses: `{'PROVED_INFINITE_ANCESTOR_DESCENT': 1}`

- `PROVED_INFINITE_ANCESTOR_DESCENT` `q == 9 mod 2^4` via `forced_burst_family`

## k19

- cubes: `4`
- infinite lift rate: `0.2500`
- statuses: `{'PROVED_INFINITE_ANCESTOR_DESCENT': 1, 'NEEDS_SPLIT': 3}`

- `PROVED_INFINITE_ANCESTOR_DESCENT` `q == 6 mod 2^6` via `forced_burst_family`
- `NEEDS_SPLIT` `q == 17 mod 2^6` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 29 mod 2^6` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 35 mod 2^6` via `no infinite lift proof found`

## k21

- cubes: `14`
- infinite lift rate: `0.0714`
- statuses: `{'PROVED_INFINITE_ANCESTOR_DESCENT': 1, 'NEEDS_SPLIT': 13}`

- `PROVED_INFINITE_ANCESTOR_DESCENT` `q == 4 mod 2^7` via `forced_burst_family`
- `NEEDS_SPLIT` `q == 66 mod 2^7` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 38 mod 2^7` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 54 mod 2^7` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 62 mod 2^7` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 65 mod 2^7` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 49 mod 2^7` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 85 mod 2^7` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 77 mod 2^7` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 61 mod 2^7` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 3 mod 2^7` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 19 mod 2^7` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 27 mod 2^7` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 31 mod 2^7` via `no infinite lift proof found`

## k24

- cubes: `35`
- infinite lift rate: `0.0571`
- statuses: `{'NEEDS_SPLIT': 33, 'PROVED_INFINITE_ANCESTOR_DESCENT': 2}`

- `NEEDS_SPLIT` `q == 196 mod 2^9` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 36 mod 2^9` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 212 mod 2^9` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 300 mod 2^9` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 2 mod 2^9` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 354 mod 2^9` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 18 mod 2^9` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 358 mod 2^9` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 214 mod 2^9` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 374 mod 2^9` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 142 mod 2^9` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 478 mod 2^9` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 126 mod 2^9` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 385 mod 2^9` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 225 mod 2^9` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 369 mod 2^9` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 293 mod 2^9` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 277 mod 2^9` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 117 mod 2^9` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 13 mod 2^9` via `no infinite lift proof found`

## k26

- cubes: `125`
- infinite lift rate: `0.0080`
- statuses: `{'PROVED_INFINITE_ANCESTOR_DESCENT': 1, 'NEEDS_SPLIT': 124}`

- `PROVED_INFINITE_ANCESTOR_DESCENT` `q == 400 mod 2^10` via `forced_burst_family`
- `NEEDS_SPLIT` `q == 200 mod 2^10` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 472 mod 2^10` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 824 mod 2^10` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 580 mod 2^10` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 452 mod 2^10` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 804 mod 2^10` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 932 mod 2^10` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 484 mod 2^10` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 660 mod 2^10` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 980 mod 2^10` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 436 mod 2^10` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 556 mod 2^10` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 684 mod 2^10` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 236 mod 2^10` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 412 mod 2^10` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 258 mod 2^10` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 386 mod 2^10` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 290 mod 2^10` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 610 mod 2^10` via `no infinite lift proof found`

## residual_certified

- cubes: `179`
- infinite lift rate: `0.0335`
- statuses: `{'PROVED_INFINITE_ANCESTOR_DESCENT': 6, 'NEEDS_SPLIT': 173}`

- `PROVED_INFINITE_ANCESTOR_DESCENT` `q == 9 mod 2^4` via `forced_burst_family`
- `PROVED_INFINITE_ANCESTOR_DESCENT` `q == 6 mod 2^6` via `forced_burst_family`
- `NEEDS_SPLIT` `q == 17 mod 2^6` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 29 mod 2^6` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 35 mod 2^6` via `no infinite lift proof found`
- `PROVED_INFINITE_ANCESTOR_DESCENT` `q == 4 mod 2^7` via `forced_burst_family`
- `NEEDS_SPLIT` `q == 66 mod 2^7` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 38 mod 2^7` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 54 mod 2^7` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 62 mod 2^7` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 65 mod 2^7` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 49 mod 2^7` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 85 mod 2^7` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 77 mod 2^7` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 61 mod 2^7` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 3 mod 2^7` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 19 mod 2^7` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 27 mod 2^7` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 31 mod 2^7` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 196 mod 2^9` via `no infinite lift proof found`

## unknown

- cubes: `228`
- infinite lift rate: `0.0000`
- statuses: `{'NEEDS_SPLIT': 228}`

- `NEEDS_SPLIT` `q == 0 mod 2^5` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 48 mod 2^6` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 40 mod 2^6` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 60 mod 2^6` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 58 mod 2^6` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 23 mod 2^6` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 80 mod 2^7` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 8 mod 2^7` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 120 mod 2^7` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 116 mod 2^7` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 76 mod 2^7` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 92 mod 2^7` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 50 mod 2^7` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 10 mod 2^7` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 26 mod 2^7` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 46 mod 2^7` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 69 mod 2^7` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 11 mod 2^7` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 103 mod 2^7` via `no infinite lift proof found`
- `NEEDS_SPLIT` `q == 119 mod 2^7` via `no infinite lift proof found`
